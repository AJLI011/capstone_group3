MAKE SURE TO READ THIS FIRST:
-make sure you have the capstone-dedd4 key in [BACKEND] folder

-in [MODELS] check if INSTORE AND ONLINE ORDERS TABLE has the fieldtype like this:

 # date_created = models.DateTimeField(auto_now_add=True) -------- Remove comment after dummy data is completed
   date_created = models.DateTimeField()


    >make sure to uncomment and remove the comment when all dummy data is created.
    >commend the other date created now without the [auto_now_add=True]

-when database is created, run python manage.py [import_staff] first to generate admin user then proceed with the [generate_instore/online] files
NOTE: start first with ALL 2023, regardless if you start online or instore. 
    >it must be generate all 2023, then generate all 2024

-----ANOTHER ALTERNATIVE TO CHECK MGA NEED AYUSIN SA MODELS IS JUST TYPE THIS AFTER PRESSING CTRL+F : Remove comment after dummy data is completed

MGA NEED I-CHECK:
INSTORE AND ONLINE ORDERS TABLE
ORDERLOGS

------
TO DELETE DATA FROM DB (FOR DUMMYDATA):
python manage.pt shell

from accounts.models import InStoreOrder, InStoreOrderItem, OnlineOrder, OnlineOrderItem, Supplier, Medicine, Inventory, Customer, Staff,OrderLog,EmployeeLog,InventoryLog

InStoreOrder.objects.all().delete()
OnlineOrder.objects.all().delete()
Inventory.objects.all().delete()
Medicine.objects.all().delete()
Supplier.objects.all().delete()
Customer.objects.all().delete()
Staff.objects.all().delete()
OrderLog.objects.all().delete()
EmployeeLog.objects.all().delete()
InventoryLog.objects.all().delete()

